from setuptools import setup

setup(
   name='clip_inspect',
   version='0.1',
   description='pay no mind :)',
   author='Paul Morris',
   author_email='pmorris2012@fau.edu',
   packages=['clip_inspect'],  #same as name
   install_requires=[], #external packages as dependencies
)
