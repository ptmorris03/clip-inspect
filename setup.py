from setuptools import setup

setup(
   name='clip-inspect',
   version='0.1',
   description='pay no mind :)',
   author='Paul Morris',
   author_email='pmorris2012@fau.edu',
   packages=['clip-inspect'],  #same as name
   install_requires=[], #external packages as dependencies
)
